title: frontent web development
description: front end web development
template:
  repo: frontent-web-development-template
  name: frontent-web-development
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
